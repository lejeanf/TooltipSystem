using UnityEngine;

namespace jeanf.tooltip
{
    /// <summary>Renders an enum field as a segmented toggle-list toolbar instead of a dropdown.</summary>
    public sealed class EnumToolbarAttribute : PropertyAttribute
    {
    }
}
